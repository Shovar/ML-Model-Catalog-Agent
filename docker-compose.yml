services:
  catalogue-agent:
    build: .
    ports:
      - "8000:8000"
    environment:
      LLM_ENDPOINT: ${LLM_ENDPOINT:-http://host.docker.internal:11434/v1}
      LLM_MODEL: ${LLM_MODEL:-qwen2.5:3b}
